// import { useContext, useState } from "react"
// import { ThemeContext, themes } from "../../contexts/theme-context"


/*
const ChronoTheme = () => {
    const { theme } = useContext(ThemeContextmeContext)

    if (theme === themes.light) {
        const chronoTheme = {
            primary: "#000000",
            secondary: "#880000",
            titleColor: "#d9eefc",
            titleColorActive: "white",
            cardBgColor: "#848e8e",
            cardTitleColor: "#000000",
            nestedCardBgColor: "#797a7a"
        }
    } else {
        const chronoTheme = {
            primary: "#ffffff",
            secondary: "blue",
            titleColor: "black",
            titleColorActive: "red",
            cardBgColor: "#ff5588",
            cardTitleColor: "red",
            nestedCardBgColor: "black"
        }
    }
}
*/

const chronoTheme = {
    primary: "#000000",
    secondary: "#880000",
    titleColor: "#d9eefc",
    titleColorActive: "white",
    cardBgColor: "#848e8e",
    cardTitleColor: "#000000",
    nestedCardBgColor: "#797a7a"
}

export { chronoTheme }